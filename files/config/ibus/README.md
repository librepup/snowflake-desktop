# IBus
## Config
Load the config for the `engine/typing-booster` by running:
```sh
dconf load /org/freedesktop/ibus/ < typing-booster.dconf
```

And export your settings via:
```sh
dconf dump /org/freedesktop/ibus/ > typing-booster.dconf
```
